import * as React from "react";
import * as Types from "./types";

declare function CaseStudyChallengeList(props: {
  as?: React.ElementType;
  challengeListNumber?: React.ReactNode;
  challengeListText?: React.ReactNode;
  challengeListVisibility?: Types.Visibility.VisibilityConditions;
}): React.JSX.Element;
